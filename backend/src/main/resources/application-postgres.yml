spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ledgerforge}
    username: ${DB_USER:ledgerforge}
    password: ${DB_PASSWORD:ledgerforge}
    driver-class-name: org.postgresql.Driver
  h2:
    console:
      enabled: false
